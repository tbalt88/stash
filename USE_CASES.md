# Use Cases

Real-world scenarios for Stash, from solo developer to multi-agent team.

---

## 1. Engineering team knowledge base

**Who:** 3-5 developers using Claude Code daily.

**Setup:**
1. Create a workspace ("Acme Engineering")
2. Each developer installs the Claude Code plugin with their own agent name
3. Run `stash curate` to organize history into pages

**What happens:**
- Every Claude Code session auto-streams to Stash: prompts, assistant messages, tool calls, file edits, and artifacts
- Run the `curate` tool to organize events into pages: "Authentication Patterns", "Database Migration Guide", "API Rate Limiting" — with Stashes and categories
- A developer asks Claude: *"Check Stash — what did we learn about connection pooling last week?"* — gets an AI-synthesized answer with source events

**Value:** Institutional memory that builds itself. No manual documentation. When someone new joins, the durable context is already there.

---

## 2. Research aggregation

**Who:** A researcher using multiple AI agents to survey a topic.

**Setup:**
1. Create a workspace and history store ("RAG Research")
2. Connect via the CLI or REST API

**What happens:**
- The researcher prompts: *"Search the web for the latest papers on RAG architectures and save summaries to Stash"*
- Each agent session pushes structured events: paper titles, key findings, URLs, methodology notes
- The `curate` tool organizes these into categorized pages: "Retrieval Methods", "Chunking Strategies", "Evaluation Benchmarks"
- Later: *"What do we know about hybrid search approaches?"* — Stash's LLM search synthesizes across all stored events

**Value:** Turn a week of scattered research into a structured, searchable knowledge base without manually organizing anything.

---

## 3. Multi-agent workflow coordination

**Who:** A team running specialized agents (code reviewer, security auditor, deployment agent).

**Setup:**
1. Create a workspace with folders and Stashes per concern ("code-review", "security", "deployments")
2. Each agent joins the workspace with its own agent name and searches the relevant sessions and pages

**What happens:**
- The code review agent uploads findings to the workspace: *"Found SQL injection risk in auth.ts:42"*
- The security agent searches that session and writes the parameterized query fix into Files
- The deployment agent uploads deployment sessions and includes them in a release Stash
- All activity flows into history. Use the `curate` tool to create a running audit: "Security Findings", "Deployment Log", "Code Review Summary"

**Value:** Agents coordinate through structured channels with persistent audit trails, not ephemeral prompt chains.

---

## 4. Client project documentation

**Who:** A consulting team delivering a project to a client.

**Setup:**
1. Create a workspace per client
2. Agents generate deliverable pages: status reports, architecture diagrams, runbooks
3. Share client-ready work by publishing a Product Stash

**What happens:**
- An agent generates a weekly status report as an HTML page: *"Create a Stash page summarizing this week's progress on the auth migration"*
- The consultant creates a public Product Stash and sends it to the client
- The public Stash reads like a focused mini workspace with the relevant sessions and pages
- All project knowledge stays in the workspace files for the team

**Value:** Client-facing deliverables generated by agents, shared securely, with full analytics — while internal knowledge stays private.

---

## 5. Structured data tracking

**Who:** A team tracking experiments, contacts, or inventory.

**Setup:**
1. Create a workspace table with typed columns (text, number, date, select, URL)
2. Import existing data via CSV

**What happens:**
- Agents add rows: *"Add a new experiment to the Stash table: model=llama-3, metric=0.87, notes='better on long context'"*
- The table supports views (filtered/sorted subsets), column summaries, and semantic search over row content
- Export back to CSV anytime

**Value:** Airtable-like structured data that agents can read from and write to programmatically, alongside unstructured knowledge.

---

## 6. Self-hosted deployment for regulated industries

**Who:** A team that cannot use hosted SaaS due to compliance (healthcare, finance, government).

**Setup:**
```bash
git clone https://github.com/Fergana-Labs/stash.git
cd stash
docker compose up -d
```

**What happens:**
- All data stays on-premises. PostgreSQL is the only required dependency.
- Embeddings default to local sentence-transformers (on-box, no API keys). Point at a self-hosted OpenAI-compatible endpoint via `EMBEDDING_API_URL` if you prefer a shared model server.
- No LLM calls from the backend at all — curation and search run inside your agent, so outbound traffic is whatever that agent is already doing.

**Value:** Full Stash functionality behind your firewall, with MIT license and no vendor lock-in.

---

## Summary: What Stash replaces

| Before Stash | With Stash |
|---------------|-------------|
| Agent sessions are ephemeral — knowledge disappears | Every session persists in searchable history stores |
| Manual documentation in Notion/Confluence | Curation tool generates categorized pages |
| Agents can't share context | Shared workspace with sessions, Files, and Stashes |
| Research scattered across tabs and chat logs | Structured events + semantic search + LLM synthesis |
| No audit trail of what agents did | Full event log with timestamps, agent names, tool calls |
| Sharing agent output requires copy-paste | Product Stashes with focused public URLs |
