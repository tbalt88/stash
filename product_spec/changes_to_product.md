The sidebar should say "Sessions / Files / Stashes" ![CleanShot 2026-05-14 at 18.39.13](codex-stash-implementation-183913.png)

we don't need the page-level breadcrumb showing what workspace we're in. we can just display what worksapce we're in at the top left of the page, like slack ![CleanShot 2026-05-14 at 18.43.15](codex-stash-implementation-184315.png)

"+ New workspace" button should only exist as a dropdown from clicking on your list of workspaces, not always viz in the sidebar

There should be buttons for "+add session" "add page (.md, .html, any file type eg pdf, pptx, .csv/table)" and "+add stash" (in the ui visually emphasize the "add stash" button)

![CleanShot 2026-05-14 at 18.50.21](codex-stash-implementation-185021.png)

there should be a back button

we should get rid of privacy tags

to be absolutely clear, stashes should go in a sidebar

breadcrumbs should be clickable

don't say "episodic" or "structured" as labels

Let's add a homepage in a new "homepage" section in the sidebar, and have this ui live there ![CleanShot 2026-05-14 at 19.05.15](codex-stash-implementation-190515.png)

the homepage should have this ![CleanShot 2026-05-14 at 19.11.50](codex-stash-implementation-191150.png)

in it, the homepage should have this top UI and then the rest bdlow that should be a place where users can write things like "about this stash"

no dragging in the sidebar the top level wiki homepage should just look like the dropbox homepage. you can drag and drop stuff within this ui. - we could allow the finder columns view? or any of the other finder views? drag to upload file/transcript should always work we should allow dragging stuff into the sidebar

can you drag a .pptx into the "session" section? - no. it has to be a .jsonl.

make this look better. no warning yellow in showing which stashes a page is in. like a beautiful list (in stashes a,b,c,d,e in a list in the right sidebar) ![CleanShot 2026-05-14 at 19.17.54](codex-stash-implementation-191754.png)

put the download button in the top right near the profile picture or as a dropdown where you can click share -> link or share -> download?

we're missing the whole ui for viewing a stash. it should look like a mini workspace, complete with a homepage and everything

Share button in the top right should be called "Share" and it should "Share the page/session as a Stash"

Just kill the page currently living at root When you click on the octopus, that should take you to the "home" page. and actually the home page should live at root.

How do we add to the wiki?

Folders should have pages. You can click on a folder to see everything in that folder. But folders are not themselves pages (this is different than notion)